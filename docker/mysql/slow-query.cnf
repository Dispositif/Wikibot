[mysqld]
slow_query_log = 1
slow_query_log_file = /var/log/mysql-slow/slow.log
long_query_time = 0.5

# Fine for local/dev where the DB gets
# rebuilt periodically; needs a real logrotate policy before this runs
# unattended for weeks on prod.
